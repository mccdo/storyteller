#
# This module is provided as storyteller_USE_FILE by storytellerConfig.cmake.  It can
# be INCLUDEd in a project to load the needed compiler and linker
# settings to use storyteller.
#

IF(NOT storyteller_USE_FILE_INCLUDED)
  SET(storyteller_USE_FILE_INCLUDED 1)

  # Update CMAKE_MODULE_PATH so includes work.
  #SET (CMAKE_MODULE_PATH
  #  ${CMAKE_MODULE_PATH}
  #  ${storyteller_CMAKE_DIR})

  # Add compiler flags needed to use storyteller.
  #SET(CMAKE_C_FLAGS "${CMAKE_C_FLAGS} ${storyteller_REQUIRED_C_FLAGS}")
  #SET(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} ${storyteller_REQUIRED_CXX_FLAGS}")
  #SET(CMAKE_EXE_LINKER_FLAGS "${CMAKE_EXE_LINKER_FLAGS} ${storyteller_REQUIRED_EXE_LINKER_FLAGS}")
  #SET(CMAKE_SHARED_LINKER_FLAGS "${CMAKE_SHARED_LINKER_FLAGS} ${storyteller_REQUIRED_SHARED_LINKER_FLAGS}")
  #SET(CMAKE_MODULE_LINKER_FLAGS "${CMAKE_MODULE_LINKER_FLAGS} ${storyteller_REQUIRED_MODULE_LINKER_FLAGS}")

  # Add include directories needed to use storyteller.
  INCLUDE_DIRECTORIES(${storyteller_INCLUDE_DIRS})

ENDIF(NOT storyteller_USE_FILE_INCLUDED)
